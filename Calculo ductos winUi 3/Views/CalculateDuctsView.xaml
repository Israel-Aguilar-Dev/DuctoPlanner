<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Calculo_ductos_winUi_3.Views.CalculateDuctsView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculo_ductos_winUi_3.Views"
    xmlns:localState="using:Calculo_ductos_winUi_3.ViewModels"
    xmlns:model="using:Calculo_ductos_winUi_3.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <!--<Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AACA48"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#AACA48"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#AACA48"/>
        </Style>-->
        
    </Page.Resources>
    
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock 
            Text="Ducto Planner" 
            Style="{StaticResource TitleTextBlockStyle}"
            Padding="36,40,36,0"
            Grid.Row="0"
            Grid.ColumnSpan="2"/>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <SelectorBar x:Name="SelectorLeftSubPage" 
                 Grid.Row="0" 
                 SelectionChanged="SelectorLeftSubPage_SelectionChanged" 
                 Margin="36,36,36,0"
                 Background="White"
                 CornerRadius="8">
                <SelectorBarItem x:Name="SelectorBarDuct" Text="Ducto" IsSelected="True"  />
                <SelectorBarItem x:Name="SelectorBarFloor" Text="Descripcion Nivel"  />
            </SelectorBar>
            <Frame x:Name="contentLeftsubPage" Grid.Row="1"  CornerRadius="8,0,0,0"/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <SelectorBar x:Name="SelectorSubPage" 
                         Grid.Row="0" 
                         SelectionChanged="SelectorSubPage_SelectionChanged" 
                         Margin="16,36,16,0"
                         Background="White"
                         CornerRadius="8">
                <SelectorBarItem x:Name="SelectorBarResumeDucts" Text="Resumen de ductos" IsSelected="True" Icon="AllApps" />
                <SelectorBarItem x:Name="SelectorBarBillOfParts" Text="Despiece" Icon="ViewAll" />
            </SelectorBar>
            <Frame x:Name="contentsubPage" Grid.Row="1"  CornerRadius="8,0,0,0"/>
        </Grid>
        
    </Grid>
</Page>
